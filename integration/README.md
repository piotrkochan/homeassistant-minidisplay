# Home Assistant Mini-Display integration

Early development tree for a local, MQTT-free Home Assistant integration and
bundled management panel.

The backend and development firmware implement the first API v1 slice documented
in `REQUIREMENTS.md`. The administrator-only `Mini Displays` sidebar panel manages
all configured displays, their pages, cards, previews, and synchronization.
